
FROM 	debian:bookworm
# check bookworm

RUN		apt-get update -y && apt-gt install -y nginx
RUN 	apt-get update -y && apt-get install -y openssl

RUN 	mkdir /etc/nginx/ssl 
# check above location
#RUN		

